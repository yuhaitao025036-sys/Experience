make_none_tensor :=
    torch.Tensor
    <- $shape list[int]
    <- $relative_to str
    # inline
    <- $dtype bfloat16
    <- torch.zeros
    <- {
        ret.st_relative_to = relative_to
        ret.st_tensor_uid = generated uuid
    }