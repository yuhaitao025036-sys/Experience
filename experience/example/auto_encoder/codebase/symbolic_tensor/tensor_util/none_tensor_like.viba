make_none_tensor :=
    torch.Tensor
    <- $input torch.Tensor
    # inline
    <- $input.shape
    <- $dtype bfloat16
    <- torch.zeros
    <- {
        ret.st_relative_to = input.relative_to
        ret.st_tensor_uid = generated uuid
    }